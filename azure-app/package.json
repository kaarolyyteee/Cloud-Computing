{
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "server": "npm start server --prefix server",
        "client": "npm start client --prefix client",
        "dev": "concurrently \"npm run server\" \"npm run client\""
    },
    "dependencies": {
        "concurrently": "^7.4.0"
    }
}
